// Phase 0 placeholder shell. The document-upload flow and report UI
// (Frontend_Specification_v2.md) are implemented in a later phase — this
// page exists only so the App Router has a valid route during the
// foundation phase.

export default function HomePage() {
  return (
    <main>
      <h1>Financial Contract Risk Reader</h1>
      <p>Phase 0 foundation — report UI not yet implemented.</p>
    </main>
  );
}
