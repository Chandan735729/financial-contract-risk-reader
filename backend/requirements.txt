fastapi==0.133.0
# Explicit pin, not left to fastapi's own unbounded `starlette>=0.40.0`
# requirement — Phase 11 dependency upgrade, closing the reachable
# starlette form-parsing DoS finding from Phase 10 (PYSEC-2026-249) at
# the root. fastapi==0.133.0 is the minimal fastapi version that supports
# starlette 1.x at all (it previously capped `starlette<1.0.0`); pinning
# starlette itself to the latest 1.x release picks up every fix in that
# family, not just the one advisory. See docs/PROVISIONAL_DECISIONS.md
# "Phase 11: FastAPI/Starlette upgrade".
starlette==1.6.0
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
python-multipart==0.0.32
pymupdf==1.28.2
python-docx==1.2.0
sentence-transformers==5.7.0
chromadb==1.5.9
rank-bm25==0.2.2
anthropic==0.121.0
